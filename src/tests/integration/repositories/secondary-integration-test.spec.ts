describe('secondary integration test', () => {
  it('secondary integration test', () => {
    const firstNumber = 5;
    const secondNumber = 6;
    const total = firstNumber + secondNumber;
    expect(total).toEqual(11);
  });
});
